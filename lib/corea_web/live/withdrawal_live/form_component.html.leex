<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "withdrawal-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :amount %>
  <%= number_input f, :amount, step: "any" %>
  <%= error_tag f, :amount %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
